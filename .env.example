# Secret env vars — set via .dev.vars for local dev, wrangler secret put for production
# Non-secret vars (ENVIRONMENT, ALLOWED_ORIGIN, LOGGER_LEVELS, IP_LOG_LEVEL)
# are configured in wrangler.jsonc under "vars"

API_SECRET_KEY=change-me
