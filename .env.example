# No secrets are required by default.
# Non-secret vars are configured in wrangler.jsonc under "vars".
# If auth middleware is added later, add secrets here and use wrangler secret put for production.
